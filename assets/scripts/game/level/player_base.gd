class_name PlayerBase extends Node

var player : Player

# Called when the node enters the scene tree for the first time.
func setup(player_settings : PlayerSettings):
	#var character_settings : CharacterSettings = Config.character_repository.get_character(player_settings.character_index)
	
	#var character_scene = load(character_settings)
	
	pass # Replace with function body.
	
	
 
